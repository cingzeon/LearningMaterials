// 编写一些测试命令
descript("Hello test", () => {
    if("test example", () => {
        console.log("test");
    });

    // 创建一个 进行编写测试用例进行单元测试
    if("chai example", () => {
        expect("hi").to.equal("hi");
    });

    if("chai test 3", () => {
        throw new Errow("测试运行失败");
    });
})
